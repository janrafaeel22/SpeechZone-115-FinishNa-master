<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.janra.speechzone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MonitoringList">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sessions" />
        <activity android:name=".sessions1" />
        <activity android:name=".sessions2" />
        <activity android:name=".goals1" />
        <activity android:name=".goals2" />
        <activity android:name=".activ1" />
        <activity android:name=".activ2" />
        <activity android:name=".comments1" />
        <activity android:name=".comments2"></activity>
    </application>

</manifest>